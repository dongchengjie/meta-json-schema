# AGENTS.md

## 目标

本仓库为 Mihomo/Clash Meta 的公开配置维护 JSON Schema。字段含义、默认值、约束和适用范围必须以 Mihomo 实际源码及其锁定依赖为依据。文档、示例和既有 schema 文案只提供线索。

## 任务路由

修改前读取所有适用文档：

- 同步 Mihomo：`docs/upstream-sync.md`、`docs/schema-maintenance.md`、`docs/conventions.md`。用户只说「同步上游」而未指定终点时，范围仍按 `docs/upstream-sync.md` 确定，不得自行假定「同步到最新」或「尽量同步更远」。
- 修改 schema、示例或 changelog：`docs/schema-maintenance.md`、`docs/conventions.md`。改写描述与改约束同等对待，都必须重新追踪消费者。
- 其他任务：只读与改动直接相关的文档。

## 强制边界

- 开始工作先检查工作树和相关历史。保留用户已有改动及未跟踪文件，不清理、覆盖或混入无关内容。
- `src/` 是 schema 的手工维护来源。`schemas/` 仅可由现有流程生成用于验证，验证后恢复，不得手工修改或提交。
- 认定字段当前有效，或认定一段描述、默认值、约束成立前，必须从公开入口追踪到最终消费者。文档、示例、旧 schema 文案、配置 tag、类型声明、内部日志名或未知字段未报错，都不能单独作为依据。
- Schema 的字段边界、归属、顺序和输入类型以公开输入的实际解析模型为准，不按转换结构、消费结构或源码目录推断。
- 写任何用户可见文案前，先按 `docs/conventions.md` 确认对象是不是用户字段，并检索职责相同的现有实现；不得用用户字段的元数据去「补全」定义对象或纯 `$ref`。
- 除非用户明确要求发布，不运行发布流程，不修改 `package.json` 版本，不创建 release 提交或 tag。
- 任务创建或下载的上游副本、依赖缓存必须位于操作系统临时目录；可只读使用已有依赖缓存，但不得依赖开发者的其他 Mihomo 工作副本或污染本仓库。
- 除上游同步流程或用户明确要求外，不自行提交。提交时只暂存当前提交所需的显式路径，不使用纳入整个工作树的暂存方式。

## 工作流程

1. 从工作树、相关历史和现有内容确定起点，识别用户已完成的部分。
2. 按任务路由审计源码依据和影响面；写文案前先确认是不是用户字段，并检索同类实现。
3. 按改动风险验证行为、引用图和工作树边界。
4. 交付时说明改动、依据、验证结果及刻意保留的用户内容；上游同步另按 `docs/upstream-sync.md` 报告范围与提交映射。

## 文档维护

本文件只保留仓库级强制规则。专题文档分工：

- `docs/upstream-sync.md`：上游副本、默认同步范围、逐提交审计与提交映射
- `docs/schema-maintenance.md`：配置审计、影响面、示例、未发布 changelog 与验证
- `docs/conventions.md`：用户字段的元数据与源码事实到 schema 的表达

同一要求只设一个权威位置，其他文档只引用。
