{
  "type": "object",
  "title": "JLS配置",
  "definitions": {
    "security-mode": {
      "required": ["username", "password"],
      "properties": { "username": { "minLength": 1 }, "password": { "minLength": 1 } }
    },
    "inactive-options": {
      "not": {
        "anyOf": [
          { "required": ["username"], "properties": { "username": { "minLength": 1 } } },
          { "required": ["password"], "properties": { "password": { "minLength": 1 } } }
        ]
      }
    }
  },
  "properties": {
    "username": {
      "type": "string",
      "title": "JLS用户名",
      "description": "JLS认证用户名",
      "markdownDescription": "`JLS`认证用户名"
    },
    "password": {
      "type": "string",
      "title": "JLS密码",
      "description": "JLS认证密码",
      "markdownDescription": "`JLS`认证密码"
    }
  },
  "anyOf": [{ "$ref": "#/definitions/inactive-options" }, { "$ref": "#/definitions/security-mode" }]
}
