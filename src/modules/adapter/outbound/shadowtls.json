{
  "type": "object",
  "title": "ShadowTLS配置",
  "definitions": {
    "compatible": { "$ref": "../../../definitions/compatible.json" },
    "security-mode": {
      "anyOf": [
        { "required": ["password"], "properties": { "password": { "not": { "const": "" } } } },
        { "required": ["version"], "properties": { "version": { "enum": [1, 2, 3] } } }
      ]
    }
  },
  "properties": {
    "password": {
      "$ref": "#/definitions/compatible/string",
      "title": "密码",
      "description": "ShadowTLS认证密码",
      "markdownDescription": "`ShadowTLS`认证密码"
    },
    "version": {
      "enum": [1, 2, 3],
      "markdownEnumDescriptions": ["ShadowTLS v1", "ShadowTLS v2", "ShadowTLS v3"],
      "title": "ShadowTLS版本",
      "description": "应与服务端保持一致",
      "markdownDescription": "应与服务端保持一致",
      "default": 2
    }
  }
}
